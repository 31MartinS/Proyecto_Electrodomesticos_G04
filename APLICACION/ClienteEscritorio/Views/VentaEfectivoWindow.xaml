<Window x:Class="ClienteEscritorio.Views.VentaEfectivoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pago en Efectivo" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Loaded="Window_Loaded">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <!-- Título -->
            <TextBlock Text="Pago en Efectivo" Style="{StaticResource MainTitle}"/>
            
            <!-- Mensaje -->
            <TextBlock Text="Total a pagar con descuento (33%):" 
                      FontSize="16" 
                      Margin="0,30,0,10"
                      HorizontalAlignment="Center"/>
            
            <!-- Campo total calculado -->
            <TextBox x:Name="TxtTotal" 
                     Style="{StaticResource PillTextBox}" 
                     Width="300"
                     IsReadOnly="True"
                     FontSize="20"
                     FontWeight="Bold"
                     TextAlignment="Center"/>
            
            <!-- Botones -->
            <Button Content="Confirmar Pago y Generar Factura" 
                    Style="{StaticResource PillButton}"
                    Background="{StaticResource GreenButtonBrush}"
                    Width="320"
                    Margin="0,30,0,0"
                    Click="BtnConfirmar_Click"/>
            
            <Button Content="Volver" 
                    Style="{StaticResource PillButton}"
                    Background="{StaticResource GrayButtonBrush}"
                    Width="200"
                    Margin="0,10,0,0"
                    Click="BtnVolver_Click"/>
        </StackPanel>
    </Grid>
</Window>
