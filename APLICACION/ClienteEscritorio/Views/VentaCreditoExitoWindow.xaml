<Window x:Class="ClienteEscritorio.Views.VentaCreditoExitoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Venta Exitosa" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Pago con Crédito" Style="{StaticResource MainTitle}"/>
            <!-- Franja verde de éxito -->
            <Border Background="{StaticResource SuccessBrush}" CornerRadius="10" Padding="15" Margin="0,30,0,20">
                <TextBlock Text="Venta Exitosa!" FontSize="18" FontWeight="Bold" Foreground="Black" TextAlignment="Center"/>
            </Border>
            <!-- Tabla de amortización -->
            <TextBlock Text="Tabla de Amortización" Style="{StaticResource SubTitle}" Margin="0,10,0,10"/>
            <DataGrid x:Name="DgAmortizacion" AutoGenerateColumns="False" IsReadOnly="True" Height="350">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="# Cuota" Binding="{Binding NumeroCuota}" Width="80"/>
                    <DataGridTextColumn Header="Valor Cuota" Binding="{Binding ValorCuota, StringFormat=C2}" Width="120"/>
                    <DataGridTextColumn Header="Interés Pagado" Binding="{Binding InteresPagado, StringFormat=C2}" Width="120"/>
                    <DataGridTextColumn Header="Capital Pagado" Binding="{Binding CapitalPagado, StringFormat=C2}" Width="120"/>
                    <DataGridTextColumn Header="Saldo" Binding="{Binding SaldoRestante, StringFormat=C2}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
            <!-- Botón Volver -->
            <Button Content="Volver" 
                    Style="{StaticResource PillButton}"
                    Background="{StaticResource GrayButtonBrush}"
                    Width="200"
                    Margin="0,30,0,0"
                    Click="BtnVolver_Click"/>
        </StackPanel>
    </Grid>
</Window>
