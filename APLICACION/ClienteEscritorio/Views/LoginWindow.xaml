<Window x:Class="ClienteEscritorio.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - Prueba Parcial 1" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            <!-- Título principal -->
            <TextBlock Text="Prueba Parcial 1" Style="{StaticResource MainTitle}"/>
            
            <!-- Subtítulo -->
            <TextBlock Text="Iniciar Sesión" Style="{StaticResource SubTitle}" Margin="0,10,0,30"/>
            
            <!-- Usuario -->
            <TextBlock Text="Usuario:" FontSize="14" Margin="0,10,0,5"/>
            <TextBox x:Name="TxtUsuario" Style="{StaticResource PillTextBox}" Width="300"/>
            
            <!-- Contraseña -->
            <TextBlock Text="Contraseña:" FontSize="14" Margin="0,15,0,5"/>
            <PasswordBox x:Name="TxtPassword" Style="{StaticResource PillPasswordBox}" Width="300"/>
            
            <!-- Mensaje de error -->
            <Border x:Name="ErrorPanel" 
                    Background="{StaticResource RedButtonBrush}" 
                    CornerRadius="5" 
                    Padding="10" 
                    Margin="0,15,0,0"
                    Visibility="Collapsed">
                <TextBlock Text="Usuario o contraseña incorrectos" 
                          Foreground="Black" 
                          FontSize="14" 
                          TextAlignment="Center"/>
            </Border>
            
            <!-- Botones -->
            <Button Content="Ingresar" 
                    Style="{StaticResource PillButton}"
                    Background="{StaticResource GreenButtonBrush}"
                    Width="200"
                    Margin="0,20,0,0"
                    Click="BtnIngresar_Click"/>
            
            <Button Content="Salir" 
                    Style="{StaticResource PillButton}"
                    Background="{StaticResource GrayButtonBrush}"
                    Width="200"
                    Margin="0,10,0,0"
                    Click="BtnSalir_Click"/>
        </StackPanel>
    </Grid>
</Window>
